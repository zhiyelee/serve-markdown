{
  "name": "serve-markdown",
  "version": "1.0.0",
  "description": "A connect middleware, parsed markdown files to html",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/zhiyelee/serve-markdown.git"
  },
  "keywords": [
    "connect",
    "markdown",
    "middleware"
  ],
  "author": "zhiyelee",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zhiyelee/serve-markdown/issues"
  },
  "homepage": "https://github.com/zhiyelee/serve-markdown",
  "dependencies": {
    "marked": "^0.3.2",
    "urldecode": "^1.0.0",
    "util-merge": "0.0.2"
  },
  "scripts": {
    "test": "mocha --reporter spec --bail --check-leaks --require should test/",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec --require should --check-leaks test/"
  },
  "devDependencies": {
    "connect": "^3.3.3",
    "istanbul": "^0.3.2",
    "mocha": "^2.0.1",
    "should": "^4.3.0",
    "supertest": "^0.15.0"
  }
}
